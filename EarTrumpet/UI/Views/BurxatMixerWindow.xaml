<Window x:Class="EarTrumpet.UI.Views.BurxatMixerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Theme="clr-namespace:EarTrumpet.UI.Themes"
        Name="WindowRoot"
        Title="Burxat's Mixer"
        Theme:AcrylicBrush.Background="AcrylicColor_Settings"
        Theme:Brush.Background="Background"
        Theme:Options.Source="App"
        MinWidth="360"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Style="{StaticResource DialogWindowStyle}"
        TextOptions.TextFormattingMode="Display"
        UseLayoutRounding="True"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <DockPanel LastChildFill="True">
            <Grid DockPanel.Dock="Top">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Margin="14,0,0,0"
                           Text="Burxat's Mixer"
                           TextAlignment="Left" />
                <Button x:Name="CloseButton"
                        Grid.Column="1"
                        Style="{StaticResource CloseButtonStyle}" />
            </Grid>
            <TextBlock Margin="24"
                       MaxWidth="360"
                       Text="Burxat's Mixer is on its way. This is where you'll be able to route each app's audio to a specific output device."
                       TextWrapping="Wrap" />
        </DockPanel>
    </Grid>
</Window>
